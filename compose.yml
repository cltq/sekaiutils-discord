services:
  bot:
    image: ghcr.io/${GITHUB_REPOSITORY:-cltq/sekaiutils-discord}:latest
    build: .
    working_dir: /app
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - BOT_CREATOR=${BOT_CREATOR}
    command: python3 bot.py
    restart: unless-stopped
